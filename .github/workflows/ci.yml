name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  ci:
    uses: ./.github/workflows/reusable-node-ci.yml
    with:
      node-version: '24'
      report-title: 'Sample Nest API CI'
      working-directory: '.'
      enable-pr-comment: true
